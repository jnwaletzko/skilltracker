<p>
  <strong>Title:</strong>
  <%= @skill.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @skill.description %>
</p>

<h2>Comments</h2>
<%= render @skill.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Back', skills_path %>
<%= link_to 'Edit', edit_skill_path(@skill) %>
